# Code owners for workistly/acpt.
#
# Do NOT turn on "Require review from Code Owners" in branch protection while there is only one
# owner — a sole owner cannot approve their own pull request, so every PR they open becomes
# unmergeable. Enable it once the other developers are on the repo and listed here.

# Default owner for everything.
*                          @frangiskos

# Areas where a wrong change costs money or leaks data. Keep these on a tighter review than the
# rest, and do not hand them to a new joiner unreviewed.
/functions/                @frangiskos
/firestore.rules           @frangiskos
/storage.rules             @frangiskos
/firebase.json             @frangiskos
/src/modules/payment/      @frangiskos
/src/pages/api/            @frangiskos
/.github/                  @frangiskos
