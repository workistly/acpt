/** Test-only stand-in for image imports, which Next.js turns into static image objects. */
const staticAsset = { src: '/test-stub.svg', height: 1, width: 1, blurWidth: 0, blurHeight: 0 }

export default staticAsset
